apiVersion: v1
kind: Service
metadata:
  name: ztx-controller
  labels:
    epc-mode: ztx-controller
spec:
  selector:
    epc-mode: ztx-controller
  ports:
    - protocol: TCP
      port: 15692   
      targetPort: 15692
      name: local
    - protocol: TCP
      port: 61592   
      targetPort: 61592
      name: external
---  
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ztx-controller-deployment
spec:
  selector:
    matchLabels:
      app: ztx-controller
  replicas: 1
  template:
    metadata:
      labels:
        app: ztx-controller
    spec:
      {{ if .Values.pcs.nodeAffinity.enabled }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: pcs-nf-type
                operator: In
                values:
                - {{ .Values.pcs.nodeAffinity.ztx | quote }}
      {{ end }}
      containers:
        - name: ztx-controller
          image: "{{ .Values.ztx.image.repository }}:{{ .Values.ztx.image.tag }}"
          imagePullPolicy: {{ .Values.ztx.image.pullPolicy }}
          securityContext:
            capabilities:
              add:
              - NET_ADMIN
              - NET_RAW
            privileged: true
            runAsGroup: 1592
          command: ["/root/5gsec/build/src/ztx"]
          env:
            - name: ENCRYPTION_ENABLED
              value: {{ .Values.ztx.encryption | default "false" | quote }}
            - name: ZTX_CONTROLLER
              value: "true"
            - name: AUTHENTICATION_ENABLED
              value: {{ .Values.ztx.authentication | default "false" | quote }}
            - name: MY_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: MY_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
